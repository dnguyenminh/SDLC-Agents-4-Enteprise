# TDD SA4E-262

Architecture: Hono routes, EntraConfig, EntraRS256Verifier, JitProvisioningService, UserRepository, SessionService with user_agent_hash.
Data model: users_sso migration, sessions.user_agent_hash.
Security: PKCE, nonce strict, user-agent binding, rate limit.
