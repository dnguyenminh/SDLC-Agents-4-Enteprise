/**
 * NativeAddonResolver — moved to resolver/ directory.
 * See ./resolver/index.ts for the implementation.
 */
export { resolveNativeBinding, resolveNativeBindingSync } from './resolver/index.js';
