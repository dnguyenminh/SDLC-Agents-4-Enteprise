export default {
  parse: (source, filePath) => {
    // Simple LWC HTML template parser stub – extracts component tags as symbols for visibility
    // For now, we return empty symbol list; file will still be indexed for count.
    return { symbols: [], relationships: [] };
  }
};
