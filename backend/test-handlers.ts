import { ModuleRegistry } from './src/modules/ModuleRegistry.js'; import { UtilityModule } from './src/modules/utility/UtilityModule.js'; import pino from 'pino'; async function run() { const logger = pino(); const registry = new ModuleRegistry(logger); registry.register(new UtilityModule(logger)); await registry.initializeAll(); console.log(registry.getToolHandlers().has('drawio_export_png')); } run();
